<% content_for :title, "Payment successful - Dealo" %>
<div class="static-page payment-success-page">
  <div class="container static-page-container">
    <h1 class="static-page-title">Payment successful</h1>
    <div class="static-page-content">
      <% if @success_type.present? %>
        <p class="static-lead payment-success-message"><%= @message %></p>
        <% if @success_type == "boost" && @listing %>
          <p class="payment-success-actions">
            <%= link_to "View your listing", @listing, class: "btn btn-payment-success" %>
          </p>
        <% end %>
        <p class="payment-success-back"><%= link_to "Back to home", root_path, class: "static-link" %></p>
      <% else %>
        <p class="static-lead"><%= @message %></p>
        <p><%= link_to "Back to home", root_path, class: "static-link" %></p>
      <% end %>
    </div>
  </div>
</div>
